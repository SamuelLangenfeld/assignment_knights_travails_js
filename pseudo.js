// PSEUDO CODE

// Search node tree using DFS

// initialize a stack
// start at the root node, push it on the stack
// while stack length > 0
// pop from stack, check if searched
// if so,
//   do nothing
// if not,
//   check if correct value,
//   if so,
//     return value
//   if not
//      label "searched" and push children onto stack
// console log "value not found"

//
//BFS
//initialize a stack
// start at the root node, push it on the stack
// while stack length > 0
// shift from stack, check if searched
// if so,
//   do nothing
// if not,
//   check if correct value,
//   if so,
//     return value
//   if not
//      label "searched" and push children onto stack
// console log "value not found"
